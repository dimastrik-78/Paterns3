using System;
using Task06._06.Observer2;
using Task06._06.Strategy;

namespace Task06._06
{
    internal class Program
    {
        public static void Main(string[] args)
        {
            // Recipe recipe = new Recipe("Курица", "Живая курица", 1);
            // RecipeObserver recipeObserver = new RecipeObserver();
            // DataRecipe dataRecipe = new DataRecipe();
            // dataRecipe.AddObserver(recipeObserver);
            // recipe.AddObserver(recipeObserver);
            // dataRecipe.SaveRecipe(recipe);
            // recipe.RemadeNameRecipe("Да");

            // Calculator calculator = new Calculator();
            // Console.WriteLine(calculator.ExecuteStrategy(Operation.Sum, 2, 2));
            // Console.WriteLine(calculator.ExecuteStrategy(Operation.Dif, 5, 2));
            // Console.WriteLine(calculator.ExecuteStrategy(Operation.Mult, 5, 5));
            // Console.WriteLine(calculator.ExecuteStrategy(Operation.Div, 10, 5));

            // Observer observer = new Observer();
            // Observable observable = new Observable();
            // observable.AddObserver(observer);
            // observable.RemadeX(5);
            
        }
    }
}